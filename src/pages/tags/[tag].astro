---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BookmarkCard from '../../components/BookmarkCard.astro';
import TagChip from '../../components/TagChip.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const bookmarks = await getCollection('bookmarks');
  const tags = [...new Set(bookmarks.flatMap(b => b.data.tags))];
  return tags.map(tag => ({
    params: { tag },
    props: {
      bookmarks: bookmarks
        .filter(b => b.data.tags.includes(tag))
        .sort((a, b) => b.data.savedAt.getTime() - a.data.savedAt.getTime()),
    },
  }));
}

const { tag } = Astro.params;
const { bookmarks } = Astro.props;
---
<BaseLayout title={`${tag}`}>
  <div class="max-w-6xl mx-auto px-6">

    <div class="mb-10 reveal">
      <a href="/link-vault/tags" class="inline-flex items-center gap-1.5 text-sm text-warm-500 hover:text-warm-950 transition-colors mb-6 font-medium">
        ← All tags
      </a>
      <h1 class="text-5xl font-bold tracking-tight text-warm-950 mb-2">{tag}</h1>
      <p class="text-warm-500 font-light">{bookmarks.length} bookmark{bookmarks.length !== 1 ? 's' : ''}</p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      {bookmarks.map(b => (
        <BookmarkCard
          slug={b.slug}
          title={b.data.title}
          description={b.data.description}
          tags={b.data.tags}
          screenshot={b.data.screenshot}
          url={b.data.url}
          savedAt={b.data.savedAt}
        />
      ))}
    </div>

  </div>
</BaseLayout>
